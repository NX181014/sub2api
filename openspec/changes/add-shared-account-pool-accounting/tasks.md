## 1. 固定口径

- [x] 1.1 记录实施基线 `59ce11c78`，确认复用 accounts、users、usage_logs 和现有 admin 权限入口
- [x] 1.2 固定 `usage_logs.total_cost` 为模型标准价值，结算时区为 `Asia/Shanghai`，币种为 CNY，周一为周起点
- [x] 1.3 固定日、周、月、自定义账期，99%价格覆盖率，单共享号池和人工转账口径
- [ ] 1.4 部署前备份生产 PostgreSQL，并导出现有账号和用量范围用于补录核对

## 2. 数据库与 Ent

- [x] 2.1 为 accounts 增加上游身份、贡献人、上传人和共享核算字段
- [x] 2.2 创建采购来源、成本流水、生命周期事件、估值汇率、结算和成员明细表
- [x] 2.3 添加金额、服务期、生命周期、换号、结算状态和外键约束及查询索引
- [x] 2.4 使用 advisory lock 保护成本服务期、正式账期互斥、结转和成本快照不重复消费
- [ ] 2.5 更新 Ent schema 与生成代码，并在 PostgreSQL 测试容器执行 191、192 号迁移及幂等验证

## 3. 账号资产与生命周期

- [x] 3.1 记录贡献人、上传人、上游身份、来源、订单、购买链接、成本服务期和质保期
- [x] 3.2 复用现有 admin 路由供可信朋友共同维护，号池 API 不接收或返回账号凭据
- [x] 3.3 实现标准化来源创建、规范化重复校验和历史引用保留
- [x] 3.4 实现购买、续费、充值、价格变更、退款、调整和换号转移的追加式成本流水
- [x] 3.5 实现确认封禁、恢复、退款、换号、报废事件，自动强信号封禁及换号关联
- [x] 3.6 将上游身份、订单号和采购链接加入审计敏感字段清理

## 4. 动态 AA 结算

- [x] 4.1 实现日、周、月、自定义区间归一和服务期自然日重叠计算
- [x] 4.2 实现成本按天摊销、服务期尾差、退款/调整和零用量结转
- [x] 4.3 实现按 `total_cost` 自动纳入实际用量成员、99%覆盖率和最大余数分币
- [x] 4.4 实现草稿重算、正式账期互斥、成本/结转防重复校验和单事务锁定快照
- [x] 4.5 实现锁定账单只读快照，且不改变余额、充值、订阅和 usage log
- [x] 4.6 添加账期归一、时区、按天摊销、零用量、分币和锁定稳定性目标测试

## 5. 回本与来源统计

- [x] 5.1 按每次请求发生日期选择 USD/CNY 汇率快照，缺失汇率时中止统计
- [x] 5.2 实现每个物理账号独立的净成本、累计价值、回本时间、当前盈亏和未回本损失
- [x] 5.3 实现封禁/恢复区间、退款和换号补价对回本结果的影响
- [x] 5.4 使用最近 14 个自然日预测回本，观察不足7日或有效用量少于3日时预测值为空
- [x] 5.5 实现来源 7/30/90 天封禁率、存活期、退款率、回本表现和样本量门槛
- [ ] 5.6 用 PostgreSQL 集成测试验证物理账号隔离、换号转入/转出和按请求日期的多段汇率

## 6. 管理页面

- [x] 6.1 新增共享号池页面，展示账号身份、贡献人、上传人、来源、质保、成本和状态
- [x] 6.2 实现成本类型与金额录入、封禁/恢复/退款/换号/报废事件和估值汇率录入
- [x] 6.3 实现日/周/月/自定义预览、覆盖率、成员责任/贡献/净额、结转和锁定确认
- [x] 6.4 实现单账号精确回本和来源质量统计，展示样本量不足状态
- [x] 6.5 接入路由、侧栏、admin 守卫和中英文 i18n，复用现有布局、表格、日期、弹窗和图表
- [x] 6.6 完成 1440x900 与 390x844 页面、弹窗和横向溢出视觉验证

## 7. 验收与部署

- [x] 7.1 运行 OpenSpec strict validate
- [ ] 7.2 运行后端目标单测、Repository 编译和 PostgreSQL 集成测试
- [x] 7.3 运行前端目标 Vitest、类型检查、lint 和生产构建
- [x] 7.4 用固定样例核对分币、单账号回本时间、自然日预测和多段汇率回本率
- [ ] 7.5 构建固定版本 Docker 镜像并完成本地健康检查
- [ ] 7.6 备份并灰度部署到当前服务器，验证代理转发、用量落库、预览、锁定和回本页面
- [ ] 7.7 为现有账号补录贡献人、来源、采购/续费流水、服务期和资产状态
